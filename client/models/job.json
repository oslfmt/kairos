{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://example.com/schemas/job.json",
  "title": "job",
  "description": "A job posting by a user. Every job has 1 of 3 statuses: posted, active, completed",
  "type": "object",
  "required": ["uuid", "title", "description", "paymentTypes", "status"],
  "properties": {
    "uuid": {
      "description": "uuid for the job",
      "type": "string"
    },
    "clientID": {
      "description": "identifier of the client who posted the job",
      "type": "integer"
    },
    "freelancerID": {
      "description": "identifier of freelancer who worked/is working on job. (only required if status == active/complete)",
      "type": "integer"
    },
    "title": {
      "description": "title of the job",
      "type": "string"
    },
    "description": {
      "description": "job description",
      "type": "string"
    },
    "price": {
      "description": "the amount that the client will pay the freelancer",
      "type": "integer"
    },
    "skills": {
      "description": "the skills the client is looking for the freelancer to have",
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "minItems": 1
    },
    "paymentTypes": {
      "description": "the list of payment types client is willing to payout",
      "type": "array",
      "items": { 
        "type": "string",
        "enum": ["Flat fee", "Pay what I want", "Endorsement", "Open to discussion"]
      },
      "uniqueItems": true,
      "minItems": 1
    },
    "status": {
      "description": "the status of the job (posted, active, complete)",
      "type": "string",
      "enum": ["posted", "active", "complete"],
      "maxItems": 1
    },
    "contract": {
      "description": "the contractID associated with the job. can be updated",
      "type": "string"
    }
  }
}